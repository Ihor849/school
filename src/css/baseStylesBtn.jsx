import  { css } from 'styled-components';

export const baseStyles = css`
  font-size: 20px;
  font-family: inherit;
  font-weight: 600;
  border: none;
  color: var(--white);
  background-color: var(--blue);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  border-radius: 15px;
  cursor: pointer;
  outline: none;
  padding: 8px 16px;
`;
export const baseStyles2 = css`
background-color: var(--red);

`